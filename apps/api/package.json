{
  "name": "@chinese-app/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy --env production",
    "type-check": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply chinese-learning-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply chinese-learning-db --remote",
    "db:migrate:remote:production": "wrangler d1 migrations apply chinese-learning-db --remote --env production",
    "db:verify:hsk1:local": "pnpm exec wrangler d1 execute chinese-learning-db --local --command \"SELECT (SELECT title_mn FROM chapters WHERE id=1) AS chapter, COUNT(*) AS lessons FROM lessons WHERE chapter_id=1\"",
    "db:count:local": "wrangler d1 execute chinese-learning-db --local --command \"SELECT (SELECT COUNT(*) FROM words) AS word_count, (SELECT COUNT(*) FROM chapters) AS chapter_count\"",
    "db:seed": "wrangler d1 execute chinese-learning-db --local --file=./src/db/seed.sql"
  },
  "dependencies": {
    "@chinese-app/srs": "workspace:*",
    "@chinese-app/types": "workspace:*",
    "fflate": "^0.8.2",
    "hono": "^4.7.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250430.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.1"
  }
}
